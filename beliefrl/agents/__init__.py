"""Agent policies for the AchieverBlocker task.

Importing this package puts lib/env on sys.path via beliefrl.env, so the
gym_minigrid primitives resolve wherever an agent module needs them.
"""

from .. import env  # noqa: F401  (imported for the sys.path side effect)
